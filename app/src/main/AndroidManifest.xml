<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chrisshelley.ctrepublic">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CTRepublic">
        <activity
            android:name=".activities.Reports"
            android:parentActivityName=".activities.Home"
            android:exported="false" />
        <activity
            android:name=".activities.ItemDetails"
            android:parentActivityName=".activities.CollectionList"
            android:exported="false" />
        <activity
            android:name=".activities.Home"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CollectionList"
            android:parentActivityName=".activities.Home"
            android:exported="false" />
    </application>

</manifest>